<%= form_for(@web_clip) do |f| %>
  <% if @web_clip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@web_clip.errors.count, "error") %> prohibited this web_clip from being saved:</h2>

      <ul>
      <% @web_clip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="bookmark">
    <div class="field">
      <%= f.label :title %><br />
      <%= f.text_field :title %>
    </div>
    <div class="field">
      <%= f.label :url %><br />
      <%= f.text_field :url %>
    </div>
    <div class="field">
      <%= f.label :quote %><br />
      <%= f.text_area :quote, :rows => 3 %>
    </div>
    <div class="field">
      <%= f.label :note %><br />
      <%= f.text_area :note, :rows => 3 %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  </div>
<% end %>
